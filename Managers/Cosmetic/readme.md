in CustomPetManager the method AnimatePetOnDeath 
we need to hook to an event should be possible,

we need an animation (i think) though thats an issue for the person using it we just need to handle it to be fair.

for the glove manager we could hook the gloves to each join on the player model (if thats how they're handled), skin manager we could hook into the Animator under the player model
i'll look later
